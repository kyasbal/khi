import { ParsePathPipe } from './parse-path.pipe';

describe('ParsePathPipe', () => {
  it('create an instance', () => {
    const pipe = new ParsePathPipe();
    expect(pipe).toBeTruthy();
  });
});
